# CLAUDE.md

## 记忆系统

优先使用 agentmemory MCP 工具进行跨会话记忆管理：

- `mem::remember` — 保存重要信息
- `mem::recall` — 检索相关记忆
- `mem::observe` — 记录观察发现
- `mem::context` — 获取会话上下文

存储位置：`D:\ai-tools\agentmemory\local.db`

## 项目信息

微信单词练习小程序，纯前端实现，使用 wx.Storage 本地存储。

详见 `docs/` 目录下的需求文档。
